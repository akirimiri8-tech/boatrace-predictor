# CLAUDE.md

このファイルは Claude Code がこのプロジェクトで作業する際に必ず参照する指示書です。
**作業前に必ず読み、ルールを守ること。**

---

## プロジェクト概要

**競艇(ボートレース)の予想精度検証・バックテスト基盤**。
まずは1〜2会場のデータで「誰が勝つか/どの着順になるか」の予想精度を
1〜2か月かけて検証し、調整しながら精度を上げていく。**最終的には舟券購入の
判断材料にすることを視野に入れる**が、それは十分な検証を経てから。

開発は以下のフェーズで進める。**前のフェーズが完了するまで次に進まない。**

1. **データ収集基盤** — Boatrace Open API から出走表・結果を取得しDBに蓄積(現在ここ)
2. **特徴量エンジニアリング** — 選手成績・モーター/ボート成績・コース別勝率・気象/波の特徴量化
3. **予想モデル構築** — まずはルールベース/回帰でスコアリング、精度が出たら機械学習へ
4. **バックテスト** — 過去データで的中率・回収率(期待値)を検証。パラメータ固定期間を設ける
5. **ペーパー予想運用** — 実際の舟券は買わず、日々の予想と結果照合をログに記録
6. **(将来)購入判断材料として活用** — 十分な検証期間・精度が確認できてから検討

### 設計方針

- **的中率だけでなく回収率(期待値)を評価軸にする**。オッズが低い本命的中より、
  期待値の高い予想を評価できる設計にする(payoutsテーブルに実際の払戻金を保存済み)。
- **対象会場は絞る**: `config.py` の `target_stadiums` = `[24, 3]`(大村・江戸川)。
  大村は内枠有利・波の影響が小さい安定会場、江戸川は汽水・強風・干満差が大きい難解会場で、
  性質の異なる2場にすることで天候/波の特徴量が実際に効くかを検証しやすくしている。
  検証が進んでから会場を広げる。
- **パラメータの走行中変更は避ける**: バックテスト・ペーパー運用のパラメータは
  一定期間固定してから見直す(trading-botプロジェクトと同じ運用ルール)。

---

## 🚨 絶対遵守の安全ルール

1. **実際の舟券購入・決済を行うコードは書かない。** このプロジェクトは予想精度検証が
   目的であり、投票所APIとの連携や自動購入機能はユーザーが明示的に依頼するまで実装しない。
2. **APIキー・アカウント情報をコードに直書きしない。** 必要になったら `.env` 経由にする。
3. **`.env`、`*.db`、`logs/` をgit追跡しない。** `.gitignore` を信頼する。
4. **バックテスト結果を検証なしに「精度が良い」と結論づけない。** サンプル数・期間・
   過学習(パラメータの後出しチューニング)の可能性を必ず言及する。
5. **未知のライブラリを勝手に追加しない。** 依存追加は `pyproject.toml` を更新し、
   理由をコミットメッセージに書く。

---

## 技術スタック

- **Python**: 3.13(winget版、`C:\Users\akiri\AppData\Local\Programs\Python\Python313\`)
- **パッケージ管理**: `pip + venv`(`.venv`)
- **データ取得**: httpx + Boatrace Open API(非公式、無料、統合api/v1は2026-01-01以降対応)
- **データ処理**: pandas, numpy
- **DB**: SQLite + SQLModel
- **設定管理**: pydantic-settings
- **ログ**: loguru
- **リトライ**: tenacity
- **テスト**: pytest

---

## データソース

- **Boatrace Open API 統合api/v1**: `https://boatraceopenapi.github.io/api/v1/{YYYY}/{YYYYMMDD}.json`
  (`today.json` で当日分も取得可)
  - **2026-01-01以降のデータにのみ対応**(それ以前は404)。旧来の programs/results 別
    リポジトリ(v3, 2023-05以降対応)は直前情報が無いため使っていない
  - 1レースに `racers`(出走表: 選手成績・モーター/ボート成績)/
    `preview`(直前情報: 発走前の実測風速・波高・展示タイム・チルト調整)/
    `result`(着順・気象実測値・払戻金)が全て含まれる
  - GitHub Actionsで定期更新。データが存在しない日付は404が返る(クライアント側で空リスト扱い)
  - 全24会場が毎日開催されるわけではない。対象会場が両方開催している日は
    `fetch_day()` で都度確認すること(2026年7月時点で概ね月の半分程度)
- **`preview`(直前情報)がレース前予想に使える気象/展示データ**。`result`側の気象値は
  レース後の実測値でバックテスト専用。天気予報(気象庁など)の統合は未着手。

---

## ディレクトリ構造とレイヤー責務

```
src/boatrace_predictor/
├── config.py       # 場コード定義、Settings(DBパス、対象会場など)
├── data/           # Boatrace Open APIクライアント、レスポンススキーマ
├── persistence/     # SQLModelスキーマ、DB保存ロジック
├── features/        # (未実装) 特徴量エンジニアリング
├── models/           # (未実装) 予想モデル
└── backtest/         # (未実装) バックテストエンジン
scripts/
└── backfill.py      # 指定期間・会場のデータを取得してDBに保存するCLI
```

**レイヤー間のルール**: `data` → `persistence` → `features` → `models` → `backtest`。
上位レイヤーは下位レイヤーを呼ぶが、逆はNG。

---

## 実行方法

```powershell
# 依存インストール(初回のみ)
.venv\Scripts\python.exe -m pip install -e ".[dev]"

# データ取得(対象会場・期間はconfig.pyのデフォルト、または--stadiumsで指定)
.venv\Scripts\python.exe scripts\backfill.py --start 2026-01-01 --end 2026-07-27

# テスト
.venv\Scripts\python.exe -m pytest
```

## コード規約

- ファイル名・関数名: `snake_case` / クラス名: `PascalCase`
- 型ヒント必須、`mypy --strict` を通す
- コメントは「なぜ」を書く。「何をしているか」は書かない
